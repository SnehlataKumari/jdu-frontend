import { NgModule } from '@angular/core';

const bootstrapImports = [

];

@NgModule({
  imports: [
    ...bootstrapImports
  ],
  exports: [
    ...bootstrapImports
  ]
})
export class BootstrapImportModule { }